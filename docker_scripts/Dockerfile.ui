# syntax=docker/dockerfile:1

# temporary image used as build environment
FROM node:24-trixie-slim AS build

WORKDIR /app
ENV PATH=/app/node_modules/.bin:$PATH

# copy required files from context
COPY package.json ./
COPY package-lock.json ./

# Configure the registry for @brainminds-dataportal to use GitHub Packages
RUN npm config set @brainminds-dataportal:registry https://npm.pkg.github.com/

# Install dependencies using Docker BuildKit secrets
# This allows us to securely pass the npm token without including it in the final image layers
RUN --mount=type=secret,id=npm_token,env=NODE_AUTH_TOKEN \
    npm config set //npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN} && \
    npm ci

# copy source code to build environment
COPY . ./

# generate ZAViewer bundles
RUN npm run build


# production environment
FROM nginx:1.30-alpine

ARG OCI_TITLE=ghcr.io/brainminds-dataportal/zaviewer
ARG OCI_DESCRIPTION="ZAViewer User Interface"
ARG OCI_SOURCE=https://github.com/brainminds-dataportal/zaviewer
ARG OCI_VERSION=dev-ui
ARG OCI_CREATED=1970-01-01T00:00:00Z
ARG OCI_REVISION=unknown

LABEL org.opencontainers.image.title="${OCI_TITLE}" \
      org.opencontainers.image.description="${OCI_DESCRIPTION}" \
      org.opencontainers.image.source="${OCI_SOURCE}" \
      org.opencontainers.image.url="${OCI_SOURCE}" \
      org.opencontainers.image.version="${OCI_VERSION}" \
      org.opencontainers.image.created="${OCI_CREATED}" \
      org.opencontainers.image.revision="${OCI_REVISION}"

# copy generated bundles and assets from building stage image
COPY --from=build /app/dist/ /usr/share/nginx/html/

RUN find /usr/share/nginx/html/ -type d -exec chmod 755 {} \;
RUN find /usr/share/nginx/html/ -type f -exec chmod 644 {} \;

# link to configuration descriptor used when displaying data located in mounted directory
RUN ln -s /usr/share/nginx/html/data/viewer.json /usr/share/nginx/html/viewer.json

# port which NginX listens to 
EXPOSE 80
